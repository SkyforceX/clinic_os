from django.apps import AppConfig


class RecordCompletionConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.record_completion"
    verbose_name = "Hoàn tất hồ sơ"
