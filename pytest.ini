[pytest]
DJANGO_SETTINGS_MODULE = tests.settings
python_files = test_*.py *_tests.py
testpaths = tests
addopts = -q --strict-markers
pythonpath = .
markers =
    smoke: fast smoke tests for routes, templates, and critical flows
